# movie-finder
im learning API and i created a movie finder with themoviedb API,and if you want to use this program you must have an API key in themovidb website

create a file called "api.py" and in that file create a variable called "api" and put your api key in that variable, for example

api = "your_API_key"
